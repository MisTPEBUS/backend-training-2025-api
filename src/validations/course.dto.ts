/**
 * 定義  Schema。
 *
 */

// Create coach 時，需要欄位

/**
 * - id: 必須為一個符合 UUID 格式的字串。
 */

/**
 * 定義新增操作參數驗證。
 * 用於驗證新增。
 */
export const CoachCreateValidated = [];

export default {
  CoachCreateValidated,
};
